node * findBeginning(node * head)
{
    node * slow = head;
    node * fast = head;

    // Find meeting point. This will be LOOP_SIZE - k steps into the linked list
    while(fast != nullptr && fast->next != nullptr)
    {
        slow = slow->next;
        fast = fast->next->next;
        if(slow == fast)
        {
            break;
        }
    }

    //Error check - no meeting point, and therefore no LOOP
    if(fast == nullptr || fast->next == null)
    {
        return null;
    }

    //Start slow back at head, keep fast at meeting point. Each are k steps from teh loop start.
    //If they end up at same place, it is loop start
    slow = head;
    while (slow != fast)
    {
        slow = slow->next;
        fast = fast->next;
    }

    //both are same
    return fast;
}
